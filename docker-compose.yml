services:
  db:
    image: mariadb:10.6
    restart: always
    environment:
      MARIADB_DATABASE: teachua
      MARIADB_USER: teachua_user
      MARIADB_PASSWORD: ${DB_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - ./db_data:/var/lib/mysql

  backend:
    image: bereh/teachua-backend:latest
<<<<<<< HEAD
    restart: always
=======
>>>>>>> main
    ports:
      - "8080:8080"
    environment:
      - JDBC_DRIVER=org.mariadb.jdbc.Driver
      - DATASOURCE_URL=jdbc:mariadb://db:3306/teachua
      - DATASOURCE_USER=teachua_user
      - DATASOURCE_PASSWORD=${DB_PASSWORD}
    depends_on:
      - db
    logging:
      driver: splunk
      options:
        splunk-token: "${SPLUNK_TOKEN}"
        splunk-url: "${SPLUNK_URL}"
        splunk-insecureskipverify: "true"
        tag: "{{.Name}}/{{.ID}}"

  frontend:
    image: bereh/teachua-frontend:latest
<<<<<<< HEAD
    restart: always
=======
>>>>>>> main
    ports:
      - "3000:3000"
    environment:
      - ROOT_SERVER=http://backend:8080/dev
      - NODE_OPTIONS=
    depends_on:
      - backend
<<<<<<< HEAD
=======
    logging:
      driver: splunk
      options:
        splunk-token: "${SPLUNK_TOKEN}"
        splunk-url: "${SPLUNK_URL}"
        splunk-insecureskipverify: "true"
        tag: "{{.Name}}/{{.ID}}"
>>>>>>> main

  nginx:
    image: nginx:latest
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - backend
    logging:
      driver: splunk
      options:
        splunk-token: "${SPLUNK_TOKEN}"
        splunk-url: "${SPLUNK_URL}"
        splunk-insecureskipverify: "true"
        tag: "{{.Name}}/{{.ID}}"
